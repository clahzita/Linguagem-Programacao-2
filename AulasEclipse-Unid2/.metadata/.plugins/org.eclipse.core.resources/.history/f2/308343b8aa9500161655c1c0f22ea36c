package br.imd.clarissa.entidades;

import java.util.Date;

import br.imd.clarissa.banco.Repositorio;

public class Aluguel {
	private Cliente cliente;
	private Veiculo veiculo;
	private Date dataAluguel;
	private Date dataDevolucao;
	private double valorPgto;
	
	public Aluguel(Cliente cliente, Veiculo veiculo, Date dataAluguel, Date dataDevolucao, double valorPgto) {
		super();
		this.cliente = cliente;
		this.veiculo = veiculo;
		this.dataAluguel = dataAluguel;
		this.dataDevolucao = dataDevolucao;
		this.valorPgto = valorPgto;
	}
	
	public cadastrarAluguel(Aluguel e){
		if(cliente.getCnh() == null && Veiculo instanceof Moto){
			if( veiculo.(Moto)getCilindrada() <= 50){
				
			}
		}
		else if (cliente.getCnh() == null && Veiculo instanceof Carro){
			
		}
		
	}
	
	
		
	
	
	
	
	
	
	
}
